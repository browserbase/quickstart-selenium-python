[project]
name = "quickstart-selenium-python"
version = "0.1.0"
description = ""
authors = [{ name = "Browserbase", email = "support@browserbase.com" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "selenium>=4.26.1",
    "browserbase>=1.6.0",
    "python-dotenv>=1.0.1",
]
